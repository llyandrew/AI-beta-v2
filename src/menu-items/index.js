// project import
import authpage from './authpage';
import dashboard from './dashboard';
import mainpage from './mainpage';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [mainpage, dashboard, authpage]
};

export default menuItems;
